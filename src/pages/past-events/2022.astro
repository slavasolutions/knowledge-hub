---
import BaseLayout from '../../layouts/BaseLayout.astro';
---

<BaseLayout title="Knowledge Hub 2022 — Employment Law for Not-For-Profits">

  <section class="page-hero">
    <div class="container">
      <span class="eyebrow">2022 Event</span>
      <h1>Employment Law for Not-For-Profits</h1>
      <p>Understanding employment standards, volunteer relationships, and HR best practices for organizations operating with limited resources.</p>
    </div>
  </section>

  <section class="section">
    <div class="container content-grid">
      <div class="content-main">
        <h2>About This Session</h2>
        <p>The 2022 Knowledge Hub explored employment law as it applies to charities and not-for-profit organizations. The session addressed the unique challenges of managing both paid staff and volunteers, and how to stay compliant with employment standards legislation.</p>

        <h2>Key Topics</h2>
        <ul class="topics-list">
          <li>Employment standards and their application to not-for-profits</li>
          <li>Volunteer vs. employee classification</li>
          <li>Hiring, termination, and severance obligations</li>
          <li>Workplace safety and harassment policies</li>
          <li>HR best practices for resource-constrained organizations</li>
        </ul>
      </div>

      <aside class="content-aside">
        <div class="info-block">
          <dl>
            <dt>Date</dt>
            <dd>April 2022</dd>
            <dt>Location</dt>
            <dd>The Old Mill, Toronto</dd>
            <dt>Format</dt>
            <dd>In-person &amp; livestream</dd>
          </dl>
        </div>
        <a href="/past-events" class="back-link">&larr; All past events</a>
      </aside>
    </div>
  </section>

</BaseLayout>

<style>
  .page-hero {
    background: var(--surface);
    border-bottom: 1px solid var(--border);
    padding: var(--sp-8) 0;
  }
  .page-hero .eyebrow { display: block; margin-bottom: var(--sp-3); }
  .page-hero h1 { font-size: 36px; margin-bottom: var(--sp-3); }
  .page-hero p { font-size: 16px; color: var(--text-secondary); max-width: 560px; line-height: 1.6; }

  .content-grid {
    display: grid;
    grid-template-columns: 1fr 280px;
    gap: var(--sp-8);
    align-items: start;
  }
  .content-main h2 { font-size: 22px; margin-bottom: var(--sp-3); }
  .content-main h2:not(:first-child) { margin-top: var(--sp-7); }
  .content-main p { font-size: 15.5px; color: var(--text-secondary); line-height: 1.65; max-width: 600px; }

  .topics-list {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: var(--sp-2);
  }
  .topics-list li {
    font-size: 15px;
    color: var(--text-secondary);
    padding-left: var(--sp-4);
    position: relative;
    line-height: 1.5;
  }
  .topics-list li::before {
    content: '';
    position: absolute;
    left: 0;
    top: 9px;
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--gold);
  }

  .content-aside {
    display: flex;
    flex-direction: column;
    gap: var(--sp-5);
    position: sticky;
    top: 80px;
  }
  .back-link {
    font-size: 14px;
    color: var(--navy);
    font-weight: 500;
    text-decoration: none;
  }
  .back-link:hover { text-decoration: underline; }

  @media (max-width: 768px) {
    .content-grid { grid-template-columns: 1fr; }
    .content-aside { position: static; }
  }
</style>
